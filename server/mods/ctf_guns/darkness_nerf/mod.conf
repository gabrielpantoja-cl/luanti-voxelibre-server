name = darkness_nerf
# Sub-mod intentionally NOT enabled in Wetlands (VoxeLibre has no `default`).
# Kept for upstream parity; safe-defused dep so a stray `load_mod_darkness_nerf`
# doesn't crash load — nothing else relies on it.
optional_depends = default
